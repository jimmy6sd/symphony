# Symphony Dashboard - Basic Authentication Configuration
# Protects dashboard pages while keeping function endpoints accessible for API integrations

# IMPORTANT: Netlify applies headers top-to-bottom, wildcard matches override specific paths
# Solution: Only protect specific paths, not /*

# Protect HTML pages
/
  Basic-Auth: symphony:${SITE_PASSWORD}

/*.html
  Basic-Auth: symphony:${SITE_PASSWORD}

# Protect assets
/styles/*
  Basic-Auth: symphony:${SITE_PASSWORD}

/src/*
  Basic-Auth: symphony:${SITE_PASSWORD}

/data/*
  Basic-Auth: symphony:${SITE_PASSWORD}

# Functions are NOT listed here, so they remain open
